# Copy this file to `.env` and fill in your key.
OPENAI_API_KEY=sk-your-key-here

# Optional overrides (sensible defaults are used if unset):
# SOKRAT_MODEL=gpt-4o-mini
# SOKRAT_EMBED_MODEL=text-embedding-3-small
# Approximate prices (USD per 1M tokens) used only for the cost read-out:
# SOKRAT_PRICE_IN=0.15
# SOKRAT_PRICE_OUT=0.60
